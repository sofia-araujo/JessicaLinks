import './App.css'
const Button = (props) => {
    return (
        // eslint-disable-next-line react/prop-types
        <a href="#"><li>{props.text}</li></a>
    )
    
}

export default Button